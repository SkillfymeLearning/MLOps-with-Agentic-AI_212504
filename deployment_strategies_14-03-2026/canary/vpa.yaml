# vpa-auto.yaml
# VPA in Auto mode - automatically adjusts pod resources
apiVersion: autoscaling.k8s.io/v1
kind: VerticalPodAutoscaler
metadata:
  name: api-server-vpa
spec:
  targetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: canary-digits-server-predictor
  updatePolicy:
    updateMode: "Auto"  # "off" / Automatically apply recommendations
    minReplicas: 2      # Keep at least 2 replicas running during updates
  resourcePolicy:
    containerPolicies:
      - containerName: kserve-container
        minAllowed:
          cpu: "50m"
          memory: "64Mi"
        maxAllowed:
          cpu: "2000m"
          memory: "4Gi"
        controlledResources:
          - cpu
          - memory
        controlledValues: RequestsAndLimits  # Adjust both requests and limits